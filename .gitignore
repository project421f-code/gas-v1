# ── Node / tooling ─────────────────────────────────────────
node_modules/
*.log
*.bak

# ── Arsip Supabase — CLI & binari besar (untuk dilanjutkan nanti) ──
# Hanya subfolder CLI/binari yang di-ignore. File .sql & backup .gs kecil
# tetap di-commit agar tidak hilang saat clone.
supabase-arsip/cli/

# ── Frontend legacy (google.script.run — tidak dipakai) ─────
# Versi lama yang hanya jalan saat di-serve dari GAS.
frontend/

# ── Keamanan: token & file pribadi ─────────────────────────
# _setup_wa_token.gs berisi token Fonnte hardcoded — JANGAN pernah di-commit.
_setup_wa_token.gs

# ── Artefak development (bukan bagian aplikasi) ────────────
# Script Python sekali pakai (fix_*.py, integrate_*.py, dll.)
*.py
# Folder tes & npm (artefak tooling lokal)
tests/
package.json
package-lock.json
# File sampah artefak Windows
nul
